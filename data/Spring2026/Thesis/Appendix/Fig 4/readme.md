Page 67. 

__Figure 4.__ Log10 weight LSEs for the networks ranked 1st (A), 7th (B), and 19th (C) when weight parameter were permuted. The dark bars represent when only w was estimated and the lighter grey bars represent when P, b, and w were all estimated, sorted by the lighter bars. Below each bar is a scheme showing input weights for each network. The values of each weight are shown: -2 (dark blue), -1 (light blue), 1 (pink), and 2 (red). *indicates the network configuration for the models shown in   Figure 4 and Table 13. The †corresponds to Fig. 11. Each horizontal line represents the median LSE of each condition. 

The `.xlsx` files here have the stacked bar charts that were used in this figure, and they contain data that was taken from the output files in each of the folders. The folders contain the original input files and their outputs that were run in Matlab. 
