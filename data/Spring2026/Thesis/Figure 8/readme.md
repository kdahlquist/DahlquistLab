Page 50. 

__Figure 8.__ Weight estimation for selected permutations of networks ranked 1st (A), 7th (B), and 19th (C) when all parameters are estimated. The †corresponds to Fig. 10. The green dagger corresponds to the network that was ranked best, the orange dagger represents a network in the middle, and the purple dagger represents the worst weight estimations. 

`.png` files are exports from GRNmap when uploading the output file from the `no constants` folder in each of the folders. The files beginning in 10 correspond to A. bimutual, beginning in 8 correspond to B. mutual cascade, and beginning in 19 correspond to C. fan in 2 autoregulation. The color in the folder names refers to the color of the dagger in Fig. 8. The data from the table is taken from the `network_optimized_weights` sheet in the output `.xslx` file in each of the folders. 
