Page 43. 

__Table 13.__ LSE between true weights and estimated weights for all 3-gene, 4-edge networks when estimating each combination of parameters. Each network was ranked based on the sum of LSE for the four conditions. Bold indicates the largest LSE per network, and italics indicates the smallest LSE per network.

`Original-Runs_overall_summary.xlsx` file has the compiled data from all of the output files in the folders. The `Table` sheet in the xlsx file has the table just as it appears in the thesis. 

Each folder number corresponds to the rank of the network from this table and in each folder there are subfolders. The forward simulation folder was used to generate the expression data from the parameters that were input. Then there is a folder with each of the input and output files when for no constants, P constant, b constant, and P and b constant. 
